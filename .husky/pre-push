#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-push checks..."

# Run type checking
npm run type-check

# Run linting
npm run lint

# Run tests if they exist
if [ -d "tests" ]; then
  echo "Running tests..."
  npm test
fi

echo "✅ Pre-push checks passed!"